const dal=require('../dal/accountDAL');class B{async getAccount(id){const profile=await dal.getProfile(id);if(!profile)throw Error('Không tìm thấy tài khoản.');const [p,s]=await Promise.all([dal.getPurchases(id),dal.getSales(id)]);return {profile,purchases:p,sales:s};}async getTransactions(id){return {purchases:await dal.getPurchases(id),sales:await dal.getSales(id)};} async updateProfile(id,d){return dal.updateProfile(id,d);} async getListings(id){return dal.getListings(id);}}module.exports=new B();
